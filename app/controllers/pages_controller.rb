# Static-ish hub pages for the nav rework. `add` is the launcher for the deck-creation
# flows (generate-a-deck, from-audio). Pure links — each row drills down into an existing
# flow.
class PagesController < ApplicationController
  def add
  end
end
